# Bundled PHP libraries

Server-side third-party PHP libraries live below this directory.

Current library:

```text
dompdf/
```

Use the official packaged Dompdf release so its own dependency set and `autoload.inc.php` remain intact.

The expected entry point is:

```text
PhpStack/lib/dompdf/autoload.inc.php
```
